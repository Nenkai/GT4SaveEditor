<Window x:Class="GT4SaveEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:GT4SaveEditor"
        mc:Ignorable="d"
        Title="GT4 Save Editor" Height="450" Width="800"
        DataContext="{x:Static local:MainWindow.Save}">
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top" Height="20">
                <MenuItem Header="File">
                    <MenuItem Header="_Load" Name="MenuItem_Load" Click="MenuItem_Load_Click"/>
                    <MenuItem Header="_Save" Name="MenuItem_Save" Click="MenuItem_Save_Click"/>
                    <Separator/>
                    <MenuItem Header="_Exit" Click="MenuItem_Exit_Click"/>
                </MenuItem>
            </Menu>
            <TabControl DockPanel.Dock="Bottom">
                <TabItem Header="Profile">
                    <Grid>
                        <Label VerticalAlignment="Top" Margin="10,10,700,0">UserName</Label>
                        <TextBox Margin="99,17,595,0" VerticalAlignment="Top" Name="label_ProfileName" MaxLength="32" Text="{Binding GameData.Profile.UserName}"/>

                        <Label VerticalAlignment="Top" Margin="10,42,710,0">Credits</Label>
                        <TextBox Margin="99,48,595,0" VerticalAlignment="Top" Name="label_Money" Text="{Binding GarageFile.Money}" TextChanged="label_Money_TextChanged"/>
                    </Grid>
                </TabItem>
                <TabItem Header="Option">
                    <Grid>
                        <xctk:PropertyGrid Name="PropertyGrid" SelectedObject="{Binding Path=GameData.Option}">
                            <xctk:PropertyGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="White" />
                                <!-- ! -->
                            </xctk:PropertyGrid.Resources>
                        </xctk:PropertyGrid>
                    </Grid>
                </TabItem>
            </TabControl>
        </DockPanel>


    </Grid>
</Window>
